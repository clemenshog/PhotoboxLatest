@using BlazorServerApp.Shared;

<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Fotobox</a>
        
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="gallery" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Gallery
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="mainScreen">
                <span class="oi oi-plus" aria-hidden="true"></span> Picture
            </NavLink>
        </div>
        @*<div class="nav-item px-3">
            <NavLink class="nav-link" href="newImage">
                <span class="oi oi-list-rich" aria-hidden="true"></span> New Picture
            </NavLink>
        </div>*@
    </nav>
</div>

@code {


    public int Index{ get; set; }
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
